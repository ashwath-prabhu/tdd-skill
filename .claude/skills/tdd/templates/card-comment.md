<!--
Paste this on the card. It's what PR review diffs the actual test file against —
keep the slice list as literal test names, since that's what should show up in the diff.
-->

**mode:** <greenfield | retrofit (characterization) | bugfix>

**seams:**
- `<file>:<exportedSymbol>` (or route/endpoint)

**slices (test names, in order):**
1. `<literal test name>`
2. `<literal test name>`

**faked at boundaries:** <what's mocked and why, or "nothing — no system boundary crossed">

**out of scope for this round (follow-up cards):** <anything deliberately deferred, or "none">
